export default {
  semi: true, // add semicolons at the end
  singleQuote: false, // use double quotes
  trailingComma: "all", // trailing commas where valid (objects, arrays, etc.)
  printWidth: 100, // wrap lines at 100 characters
  tabWidth: 2, // 2 spaces indentation
  bracketSpacing: true, // spaces between brackets { foo: bar }
  arrowParens: "always", // always include parens for arrow functions
};
